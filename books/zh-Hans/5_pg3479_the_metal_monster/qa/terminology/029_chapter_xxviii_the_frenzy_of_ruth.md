# 029_chapter_xxviii_the_frenzy_of_ruth 术语 QA

terminology_status: PASS

## 检查点

- Ruth 统一为 `露丝`，Norhala 统一为 `诺哈拉`，Drake 统一为 `德雷克`，Ventnor 统一为 `文特诺`。
- Metal Monster 译为 `金属巨兽`；metal 按语境译为 `金属`、`金属大军`、`金属生命`等，未机械译成“金属物”。
- shapes 统一倾向 `形体`；forms 按语境为 `形态`、`形状`。
- Pit 译为 `深坑`，City 译为 `城市`，lightnings 译为 `闪电`。

结论：术语一致性 PASS。
