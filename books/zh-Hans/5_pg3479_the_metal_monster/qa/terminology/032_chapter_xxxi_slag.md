# 032_chapter_xxxi_slag 术语 QA

terminology_status: PASS

## 检查点

- Slag 统一译为 `熔渣`；Metal Monster 译为 `金属巨兽`。
- Keeper 译为 `守护者`，Disk 译为 `圆盘`，Ruler 译为 `统治者`。
- shapes 译为 `形体`；Hordes 译为 `大军`；Pit 译为 `深坑`。
- 地名 Gyantse 译为 `江孜`，Trans-Himalayan 译为 `跨喜马拉雅`。

结论：术语一致性 PASS。
