# 030_chapter_xxix_the_passing_of_norhala 术语 QA

terminology_status: PASS

## 检查点

- Norhala 统一为 `诺哈拉`，Keeper 译为 `守护者`，Metal Emperor 译为 `金属帝王`。
- cones 结合本章场景译为 `群锥`；Disk 译为 `圆盘`；Cross 译为 `十字`。
- Pit 译为 `深坑`，City 译为 `城市`，Hordes 译为 `大军`。
- lightnings 译为 `闪电`，orbs 按语境译为 `球体`或`发光体`。

结论：术语一致性 PASS。
