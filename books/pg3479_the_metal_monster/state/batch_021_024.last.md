已完成 21-24 章：

- 写入 `chapters/translated/` 同名中文译稿
- 写入 `chapters/final/` 同名终稿
- 每章补齐 6 类 QA：`chapter_controls`、`fidelity`、`readability`、`imagery`、`terminology`、`gates`
- 所有 QA 均为 `PASS`

验证结果：

- 译稿与终稿四章 SHA-256 一致
- 终稿标题为 `display_title`，副题为中文 `subtitle`
- 扫描终稿/译稿无英文残留、乱码字符、旧式分隔符或异常空格
- 段落块数量均为源文 +1，符合终稿拆分为章号标题 + 副题的结构
- 未运行 EPUB 构建
- 未修改 `template`、其他书、`qa/pretranslation`、`qa/samples`、`state/pipeline_state.json`