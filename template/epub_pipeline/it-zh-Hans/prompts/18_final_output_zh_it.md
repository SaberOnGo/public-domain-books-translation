# 18 最终输出

最终输出前必须确认：全章 gate PASS、EPUBCheck fatal/error 为 0、publication lint 无硬错误、asset manifest 无硬错误、reader-facing check 无硬错误、随机抽检 `review:random-validate:pass` 通过、release 创建成功。`output/book.epub` 不是最终 release。
